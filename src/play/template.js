import { dContainer } from '../asprite';

export default function Play(play, ctx) {

  const container = dContainer();
  const initContainer = () => {
  };
  initContainer();

  this.init = data => {
  };

  this.update = delta => {
  };

  this.render = () => {
  };

}
